# 测试版验证记录

验证日期：2026-09-10。Windows / Node.js 24.12 / Chrome，针对生产构建；手机检查采用 390 × 844 浏览器视口，桌面为 1440 × 900，不代表真机测试。

| 检查 | 结果 |
| --- | --- |
| TypeScript、Vite 生产构建与 PWA 清单 | 通过 |
| 内容校验 | 19 项通过；5 条个人档案、5 类，每类 1 条 |
| 视口规则与发布检查 | 通过；相对路径、照片原文件校验、个人字段扫描 |
| 桌面进入页、跳过开场、三维档案阵列 | 已打开并截图检查 |
| 完整开场 | 手机视口播放并进入个人档案详情 |
| 个人概览 → 项目经历档案 | 正确进入 X-003 |
| 详情内容页签、收藏 | 切换成功；收藏状态和检索结果标记更新 |
| 档案检索 | 项目经历涵盖 AstrBot、数学课考卷合集与 MaaEnd |
| 模型查看 | 打开、拆解、重组、Esc 返回已实测 |
| 独立阅读页 | 全页截图检查；复制邮箱成功；无 canvas，仅加载阅读页脚本 |
| 手机布局 | 进入页、阅读页、档案详情与个人概览检查；修复顶部导航重叠 |
| 子目录托管 | `/homepage-rhine-lab/dist/?file=X-003` 正常加载并打开指定档案；资源无 HTTP 400+；页面宽度 390 与视口一致 |
| Windows 启停脚本 | 启动、停止、重启、重复启动识别均通过 |
| 发布压缩包 | 约 30.8 MiB，797 个文件；包含两种入口、模型、字体、SW 与档案导出 |

浏览器无 JavaScript 错误。旧阴影模式已更新为 PCFShadowMap；构建保留 Three.js 主包体积提示。三维入口资源较大，低性能设备可使用独立阅读页。开场在窄屏采用横向影片居中展示，保留原版构图。

本地截图位于 `output/playwright/`（不提交）；仓库预览见 `docs/preview/`。正式主页仓库未修改；本项目没有 Pages 或自动上线流程。

## 2026-09-10 第二版修订（历史记录）

- PERSONAL 改为正常字体排版，移除沿用其他单词的逐字坐标；桌面与 390px 手机截图确认。
- 五份档案分别讲基本介绍、教育背景、项目经历、校园经历、联系方式；导出目录清除旧模块文件。
- 默认性能画质，场景动画最高约 30 帧/秒；8 秒空闲后冻结；弹窗覆盖时冻结；隐藏页面停止帧调度。
- 旧版默认设置迁移为新性能默认值；此后手动选择的画质正常保存。
- 内容 19 项、渲染调度 2 项、视口与发布检查通过。

同一 Chrome 桌面视口 1440 × 900、基本介绍详情页，应用内 Three.js 计数：

| 指标 | 修订前 | 修订后 |
| --- | ---: | ---: |
| 持续动画帧率 | 240 fps | 实测约 27 fps，上限 30 |
| 单帧绘制调用 | 102 | 48 |
| 单帧三角形 | 5,330,799 | 2,638,231 |
| 实际绘图尺寸 | 1440 × 900 | 1296 × 810 |
| 无操作后 | 持续渲染 | 帧计数停在 209，多次采样不变 |
| 个人概览覆盖时 | 持续渲染 | 帧计数停在 419，多次采样不变 |

这些是同机浏览器内计数，不等同于系统 CPU/GPU 百分比，也不是所有设备的性能保证。照片与三维模型保留，画质设置可手动提高。

## 后续修订：简历字段与流畅度

档案摘要区按内容采用姓名、学校、学院、专业、在校时间、参与项目、参与方式、个人贡献、组织、职务、任职时间及联系方式等字段；TXT 导出使用相同字段。

动画目标由 30 调整为 60 帧/秒，移除每帧额外定时等待；保留空闲、遮挡和后台暂停。循环场景模型由 288 减为 160，复用原开场的五列模型。应用内实测基本介绍单帧三角形降至 1,538,967；循环切列和档案显示已检查。实际帧率仍随设备和画质变化。

## 2026-09-11 滚轮切换与正式主页入口

- 详情页滚轮驱动 `closing → moving → opening → idle`，每阶段根据模型姿态完成情况推进。
- 应用内浏览器实测 X-001 → X-002 → X-003；额外连续滚轮输入没有跳过档案；反向滚动成功从 X-003 回到 X-002。
- 长正文滚动时停留在项目经历，移动端阅读容器 scrollTop 从 0 增至 530，未误切档案。
- 自动切换期间持续渲染，保留 60 帧目标；Escape 或手动操作可取消序列。2 项状态机测试覆盖阶段顺序、滚轮连发、反向循环与取消。
- 封面概览链接实测跳转到 `https://alphazero78.github.io/`，显示正式主页原有内容与版式。
